name: Angular CI
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ${{ github.workflow }}-apps-employee-front-end-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./apps/employee-front-end
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: ./apps/employee-front-end/package-lock.json

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build
        run: npm run build

      - name: Test
        # Remove 'continue-on-error' when your tests are stable
        run: npm run test --if-present -- --watch=false --browsers=ChromeHeadless
        continue-on-error: true
      - name: (Test failures ignored)
        run: echo "Tests completed – failures ignored by DevOpsGen"
